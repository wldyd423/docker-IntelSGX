# This bomb is used for ubuntu16.04 
(which is the expected OS for sgx-2.2 used by this project)

ISSUES:

stdarg.h: No such file or directory

This header is included by the few headers used by the phy-module.